<USER_REQUEST>
I am working on the FreightTiger Software Engineering Intern (AI)
24-Hour Case Study.

First analyze the project requirements and datasets before implementing
the solution.

Read:
- FreightTiger_Intern_CaseStudy.pdf
- data/shipment_records.csv
- data/context_notes.csv
- data/sample_output_format_v2.csv

Do NOT build the full application yet.

Create DATA_ANALYSIS.md containing:

1. Shipment dataset
   - column names
   - data types
   - row count
   - missing values
   - date range
   - unique routes
   - route types

2. Context notes
   - note structure
   - note IDs
   - route applicability
   - dates
   - whether each note describes an actual cost-impacting event,
     a non-impacting event, or a return to normal conditions

3. Output contract
   - exact column names
   - exact column order
   - examples
   - expected formatting

4. Explain the required calculation:

weekly cost_per_tonne_km =
SUM(freight_cost_inr) /
SUM(quantity_tonnes * distance_km)

Weeks are Monday-Sunday and week_of must be Monday.

5. Explain own-history baseline:
trailing average of up to 8 prior weekly cost_per_tonne_km values,
strictly excluding the current week.

6. Explain peer baseline:
same-week average cost_per_tonne_km of all OTHER routes sharing the
same route_type. Exclude the current route.

7. Identify ambiguous requirements, especially the anomaly threshold.
Do not invent requirements that aren't specified.

8. Identify hallucination risks in context_notes.csv.

Do not modify any source files.
Do not implement the LLM yet.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-24T07:32:53+05:30.
</ADDITIONAL_METADATA>
<USER_SETTINGS_CHANGE>
The user changed setting `Model Selection` from None to Gemini 3.6 Flash (High). No need to comment on this change if the user doesn't ask about it. If reporting what model you are, please use a human readable name instead of the exact string.
</USER_SETTINGS_CHANGE>